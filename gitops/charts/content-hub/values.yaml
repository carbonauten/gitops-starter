replicaCount: 1

image:
  repository: ghcr.io/carbonauten/content-hub
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

env:
  entraMockAuth: "false"
  redirectUri: "https://content-hub.example.com/api/auth/callback"

secrets:
  sessionSecret: ""
  azureTenantId: ""
  azureClientId: ""
  azureClientSecret: ""

resources: {}
