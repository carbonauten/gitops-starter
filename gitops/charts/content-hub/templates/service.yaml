apiVersion: v1
kind: Service
metadata:
  name: {{ include "content-hub.fullname" . | trunc 63 | trimSuffix "-" }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
  selector:
    app.kubernetes.io/name: {{ include "content-hub.name" . }}
