apiVersion: v1
kind: Service
metadata:
  name: {{ include "plc-api.fullname" . | trunc 63 | trimSuffix "-" }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8081
  selector:
    app.kubernetes.io/name: {{ include "plc-api.name" . }}

